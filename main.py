import sys
import numpy as np
import pandas as pd
import itertools as iter

b = np.array([[]])
b = np.append(b, [[11, 13]], axis=1)
b = np.append(b, [[12, 14]], axis=0)
b = np.append(b, [[19, 19]], axis=0)
print(len())